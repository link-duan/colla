{
  "name": "colla-workspace",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "check:registries": "node scripts/check-registries.mjs",
    "check:versions": "node scripts/check-versions.mjs",
    "measure": "pnpm --filter @colla/core measure",
    "release:preflight": "pnpm check:versions && pnpm check:registries && cargo publish -p colla --registry crates-io --dry-run && pnpm --filter @colla/core exec npm publish --dry-run --registry https://registry.npmjs.org",
    "test:js": "pnpm --filter @colla/core test"
  }
}
